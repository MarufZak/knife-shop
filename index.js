const introSlider = new Swiper('.swiper',{
    pagination: {
        el: '.swiper-pagination',
        dynamicBullets: true
    },
    grabCursor: true
});

// const images = document.querySelectorAll('img');
// images.forEach(item=>{
//     img.ondragstart = () => {
//         return false;
//     };
// })
